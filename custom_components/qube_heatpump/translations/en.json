{
  "config": {
    "abort": {
      "already_configured": "Device is already configured."
    },
    "error": {
      "cannot_connect": "Failed to connect. Check host and port."
    },
    "step": {
      "user": {
        "title": "Qube Heatpump",
        "description": "Enter the heat pump IP or host.",
        "data": {
          "host": "Host/IP"
        }
      }
    }
  }
  ,
  "options": {
    "step": {
      "init": {
        "title": "Qube Heat Pump Options",
        "description": "Set advanced options. Unit/Slave ID selects which device behind a Modbus/TCP gateway to read from. If you have multiple devices at the same IP, create one integration entry per unit ID. You can also choose to display the vendor's unique names instead of friendly names.",
        "data": {
          "unit_id": "Unit/Slave ID",
          "use_vendor_names": "Use vendor names for display",
          "label": "Hub label (e.g., qube1)",
          "show_label_in_name": "Show hub label in entity names"
        }
      }
    }
  }
}
